generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Universal Unique ID
model User {
  id   String @id @default(uuid())
  name String
}
